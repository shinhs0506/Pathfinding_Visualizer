#include "Algorithms.h"

class BFS : public Algorithms {
    public:
        std::vector<std::pair<int, int>> solve(std::vector<int, int> *board);
};

std::vector<std::pair<int, int>> BFS::solve(std::vector<int, int> *board) {
    std::vector<std::pair<int, int>> res;
    return res;
}
